/* 430~1400사이 */
    font-size: clamp(1.5rem, 3.5vw, 1.6rem); /* clamp(모바일(최소값), 유동, 웹(최대값) */

    /* 
        1vw = 화면너비의 1%
        화면이 320일경우 
        1vw = 3.2px - 320의 1%
        우리가 선언한 3.5vw
        3.5 * 3.2 = 11.2px

        화면이 768px
        1vw = 7.68px
        7.68*3.5 = 13.76px 대략 24px
        하지만 최대값이 16px로 제한되어있기 때문에 글자크기를 16픽셀보다 커지지 않음.
    */